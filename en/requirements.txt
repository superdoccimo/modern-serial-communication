# Modern Serial Communication - Dependencies
# Python Serial Communication Library Requirements

# 基本的な依存関係
setuptools>=65.0.0
pyserial>=3.5
pyserial-asyncio>=0.6
textual>=0.41.0,<0.50.0  # バージョンを制限
rich>=13.0.0

# Windows環境での推奨
# colorama>=0.4.4  # Windows環境でのカラー出力

# Optional: For future MQTT support
# asyncio-mqtt>=0.11.1

# Optional: For future web interface
# fastapi>=0.68.0
# uvicorn>=0.15.0
# websockets>=10.0

# Optional: Windows向けWin32 APIサポート
pywin32>=306
